<% if user_signed_in? %>
  <%= link_to 'My Account', edit_user_registration_path, class: 'text-white no-underline font-sans mx-2 px-4 py-2 border rounded border-purple-light   hover:text-purple hover:bg-white hover:border-white '%>
<% else %>
  <%= link_to 'Register', new_user_registration_path, class: 'text-white no-underline font-sans mx-2 px-4 py-2 border rounded border-purple-light  hover:text-purple hover:bg-white hover:border-white '%>
<% end %>